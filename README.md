This contains all my dotfiles. As im learning will be adding continusly in it.

### So how do i keep differnt package dotfiles in one folder?
Easy, just use **symbolink** links.

`ln -s [target] [destination]`
